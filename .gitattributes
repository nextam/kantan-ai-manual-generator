*.pt filter=lfs diff=lfs merge=lfs -text

# Git attributes for consistent line endings
# Text files should be normalized and use LF on checkout
* text=auto

# Shell scripts must use LF line endings for Unix compatibility
*.sh text eol=lf
*.bash text eol=lf

# Python files should use LF
*.py text eol=lf

# Configuration files
*.json text eol=lf
*.yml text eol=lf
*.yaml text eol=lf
*.conf text eol=lf

# Dockerfile and related
Dockerfile text eol=lf
*.dockerfile text eol=lf
